export const test = () => {
  // vulnerable_app
  const sqlite3 = require("sqlite3").verbose();
  const fs = require("fs");
  const API_KEY = "sk_live_123456789";

  const a: number = 5;
  const b: number = 10;
  const sum: number = a + b;
};
